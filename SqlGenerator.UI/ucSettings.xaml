<UserControl x:Class="SqlGenerator.UI.ucSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SqlGenerator.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl MaxHeight="300">
        <TabItem Header="General">
            <StackPanel Orientation="Horizontal" Margin="0,10,0,35">
                <Label MinWidth="60">Author:</Label>
                <TextBox Name="txtAuthor" VerticalAlignment="Center" MinWidth="250"></TextBox>
            </StackPanel>
        </TabItem>
        <TabItem Header="Stored procedures">
            <TabControl MaxHeight="300">
                <TabItem Header="General">
                    <StackPanel>
                        <Label>Select role(s) to grant "execute":</Label>
                        <ListBox x:Name="lstRoles" MaxHeight="200" MinHeight="150" SelectionMode="Multiple"></ListBox>
                    </StackPanel>
                </TabItem>
            </TabControl>
        </TabItem>
        <TabItem Header="Entities">
            <StackPanel Orientation="Horizontal" Margin="0,10,0,35">
                <Label MinWidth="60">Entities project namespace:</Label>
                <TextBox Name="txtEntitiesNamespace" VerticalAlignment="Center" MinWidth="250"></TextBox>
            </StackPanel>
        </TabItem>
        <TabItem Header="Repositories"></TabItem>
    </TabControl>
</UserControl>
